import { CalcularPuntuacionPipe } from './calcular-puntuacion.pipe';

describe('CalcularPuntuacionPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcularPuntuacionPipe();
    expect(pipe).toBeTruthy();
  });
});
