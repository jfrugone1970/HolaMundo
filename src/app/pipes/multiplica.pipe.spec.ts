import { MultiplicaPipe } from './multiplica.pipe';

describe('MultiplicaPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplicaPipe();
    expect(pipe).toBeTruthy();
  });
});
