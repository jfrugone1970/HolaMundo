import { CalculaVentasPipe } from './calcula-ventas.pipe';

describe('CalculaVentasPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculaVentasPipe();
    expect(pipe).toBeTruthy();
  });
});
