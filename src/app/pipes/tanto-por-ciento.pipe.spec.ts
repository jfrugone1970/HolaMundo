import { TantoPorCientoPipe } from './tanto-por-ciento.pipe';

describe('TantoPorCientoPipe', () => {
  it('create an instance', () => {
    const pipe = new TantoPorCientoPipe();
    expect(pipe).toBeTruthy();
  });
});
