import { AreaTrianguloPipe } from './area-triangulo.pipe';

describe('AreaTrianguloPipe', () => {
  it('create an instance', () => {
    const pipe = new AreaTrianguloPipe();
    expect(pipe).toBeTruthy();
  });
});
